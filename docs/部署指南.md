# Mini RPC 部署指南

## 1. 文档目标

本指南只负责一件事：让零经验同学也能把 Mini RPC 在本地跑起来。  
架构原理请看《架构设计白皮书》，技术取舍请看《技术选型最佳实践》。

## 2. 适用范围

- Windows 10/11（优先）
- macOS/Linux（命令同理）
- JDK 17+
- Maven 3.9+

## 3. 前置环境检查

### 3.1 安装 JDK

```bash
java -version
```

预期：输出版本号且主版本不低于 17。

### 3.2 安装 Maven

```bash
mvn -version
```

预期：输出 Maven 版本信息，并识别到本机 Java 环境。

### 3.3 获取项目代码

```bash
git clone <你的仓库地址>
cd mini-RPC
```

## 4. 第一次启动（推荐流程）

### 4.1 拉取依赖并编译

```bash
mvn clean package -DskipTests
```

预期：所有模块编译成功，出现 `BUILD SUCCESS`。

### 4.2 使用 IDEA 导入项目（新手必做）

1. 打开 IDEA，点击 `Open`，选择项目根目录 `mini-RPC`。  
2. 等待右下角 Maven 导入完成。  
3. 打开 `File -> Project Structure -> Project`，确认 `Project SDK = JDK 17`。  
4. 打开 `Settings -> Build, Execution, Deployment -> Build Tools -> Maven`，确认 Maven 可用。  
5. 在右侧 Maven 面板执行一次 `Lifecycle -> clean`、`Lifecycle -> package`。  

预期：`package` 阶段成功，且 `rpc-core/rpc-client/rpc-server/rpc-demo` 都编译通过。

### 4.3 在 IDEA 启动服务端

1. 打开文件 `rpc-demo/src/main/java/com/minirpc/demo/ServerBootstrap.java`。  
2. 点击 `main` 方法左侧绿色三角，选择 `Run 'ServerBootstrap.main()'`。  
3. 看到控制台输出 `server started at 127.0.0.1:9000` 即成功。  

### 4.4 在 IDEA 启动客户端

1. 保持服务端窗口不要关闭。  
2. 打开文件 `rpc-demo/src/main/java/com/minirpc/demo/ClientBootstrap.java`。  
3. 点击 `main` 方法左侧绿色三角，选择 `Run 'ClientBootstrap.main()'`。  
4. 客户端控制台输出 `rpc result: hello mini-rpc` 即调用成功。  

### 4.5 纯命令行启动（可选）

如果你暂时不想在 IDEA 配置 Run Configuration，可以使用命令行：

启动服务端：

```bash
java -cp "rpc-core\target\classes;rpc-server\target\classes;rpc-demo\target\classes" com.minirpc.demo.ServerBootstrap
```

新开终端启动客户端：

```bash
java -cp "rpc-core\target\classes;rpc-client\target\classes;rpc-demo\target\classes" com.minirpc.demo.ClientBootstrap
```

预期：客户端输出 `rpc result: hello mini-rpc`。

## 5. 最小自检清单

- `mvn clean package -DskipTests` 成功
- 服务端输出 `server started at 127.0.0.1:9000`
- 客户端输出 `rpc result: hello mini-rpc`
- 服务端不断开时，客户端可重复调用成功
- 改动 `HelloServiceImpl` 返回值后，重新运行能看到新结果

## 6. 常见启动失败与快速处理

- 依赖下载失败：检查网络与 Maven 镜像配置
- 端口占用：修改启动端口或结束占用进程
- Java 版本不匹配：统一为 JDK 17+
- 主类找不到：确认模块名、包名、启动类全限定名
- IDEA 无法识别 Maven 项目：右键根 `pom.xml` 执行 `Add as Maven Project`
- IDEA 运行时报 `ClassNotFoundException`：先执行一次 `mvn clean package -DskipTests`
- 客户端报连接失败：先确认服务端进程仍在运行，再检查端口 `9000`

详细问题请看《故障排查及解决文档》。

## 7. Day1 代码定位索引（便于跟读）

- 服务端入口：`rpc-demo/.../ServerBootstrap`
- 客户端入口：`rpc-demo/.../ClientBootstrap`
- 协议模型：`rpc-core/.../RpcRequest`、`rpc-core/.../RpcResponse`
- 客户端代理：`rpc-client/.../RpcClientProxy`
- 服务端执行：`rpc-server/.../RpcServer`

建议阅读顺序：`ClientBootstrap -> RpcClientProxy -> RpcRequest -> RpcServer -> RpcResponse`。

## 8. 版本同步说明

本指南跟随主分支更新。每次新增启动方式、端口调整、模块变更，都需要同步更新本文件与《变更日志》。
